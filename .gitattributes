# Snapshot files must use LF line endings on all platforms. Snapshot.Verify normalizes the
# received value to LF and then compares the verified file byte for byte, so a snapshot checked
# out as CRLF on Windows would never match.
*.verified.txt text eol=lf
*.received.txt text eol=lf
